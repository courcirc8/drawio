import {cleaningProposals,cleaningMetrics,cleaningCandidate} from '../lib/cleaning.js';
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {spawnSync} from 'node:child_process';
import {parseDrawio,getPage,allCells,cellInfo,serialize,addVertex} from '../lib/model.js';import {parseSpice,extractNetlist} from '../lib/netlist.js';import {compare} from '../lib/lvs.js';import {auditVisibleConnectivity} from '../lib/visible-connectivity.js';import {directGatePortJogs} from '../lib/signal-alignment.js';import {exportDocument,closeBrowser} from '../lib/render.js';
import {routeMetrics} from '../lib/annotation-refinement.js';
import {feedbackPenalty} from '../lib/feedback-refinement.js';
const [input,output,loopArg]=process.argv.slice(2),loop=Number(loopArg);
if(!input||!output||![1,2,3,4,5,6].includes(loop)||fs.existsSync(output))throw new Error('INPUT NEW_OUTPUT LOOP(1..6) required');
fs.cpSync(input,output,{recursive:true});const rows=JSON.parse(fs.readFileSync(path.join(output,'summary.json'))),log=[];
function evaluate(p,ref,id,name){const model=getPage(parseDrawio(fs.readFileSync(p+'.xml','utf8'))),lvs=compare(extractNetlist(model),ref),visual=auditVisibleConnectivity(model),run=spawnSync('python3',[fileURLToPath(new URL('./check.py',import.meta.url)),p+'.xml','--json'],{encoding:'utf8',timeout:60000});if(![0,1].includes(run.status))throw new Error(run.stderr||'Checker failed');const check=JSON.parse(run.stdout);return {id,lvs,visual,check,gatePortJogs:directGatePortJogs(model),routing:routeMetrics(model),feedback:feedbackPenalty(model,ref),cleaning:cleaningMetrics(model),eligible:lvs.match===true&&visual.visible_connectivity_match===true};}
function rank(r){const c=r.cleaning;return [r.check.errors,r.feedback.clearance,...(loop===1?[c.groundSpread]:loop===2?[c.io]:loop===3?[c.detour]:loop===4?[c.passiveJog]:[c.detour,c.io,c.passiveJog]),r.routing.portBends,r.feedback.shortJogs,r.routing.bends,r.check.warnings,r.check.crossings,r.routing.length];}
const better=(a,b)=>{const aa=rank(a),bb=rank(b);for(let i=0;i<aa.length;i++)if(aa[i]!==bb[i])return aa[i]<bb[i];return false;};
try{for(const row of rows){const dir=path.join(output,row.name),ref=parseSpice(fs.readFileSync(row.source,'utf8')),before=row.selected;let best=evaluate(path.join(dir,before),ref,before,row.name),initial=best;if(!best.eligible||best.check.errors)throw new Error('Invalid initial '+row.name);
 let attempts=0;for(let round=1;round<=2;round++){const parent=best.id,xml=fs.readFileSync(path.join(dir,parent+'.xml'),'utf8'),model=getPage(parseDrawio(xml));const proposals=cleaningProposals(model,ref,loop).filter(p=>!process.env.CLEAN_INTENTS||!['input-left','io-clearance'].includes(p.kind)||JSON.parse(fs.readFileSync(process.env.CLEAN_INTENTS,'utf8'))[row.name]?.inputsLeft===true);
 for(const proposal of proposals.filter(p=>!process.env.CLEAN_ONLY||process.env.CLEAN_ONLY.split(',').includes(p.kind))){const id=`${path.basename(output)}-${round}-${proposal.id}`,p=path.join(dir,id);attempts++;
 try{fs.writeFileSync(p+'.xml',cleaningCandidate(xml,proposal));let ev=evaluate(p,ref,id,row.name);const repairs=ev.check.violations.filter(v=>v.rule==='dot-2way'||v.rule==='30');if(repairs.length){const doc=parseDrawio(fs.readFileSync(p+'.xml','utf8')),m=getPage(doc);let seq=0;for(const v of repairs){if(v.rule==='dot-2way')for(const el of allCells(m)){const c=cellInfo(el);if(c.style.map.get('contactDot')==='1'&&Math.hypot(c.x+c.w/2-v.at[0],c.y+c.h/2-v.at[1])<.1)el.parentNode.removeChild(el);}else if(!allCells(m).map(cellInfo).some(c=>c.style.map.get('contactDot')==='1'&&Math.hypot(c.x+c.w/2-v.at[0],c.y+c.h/2-v.at[1])<3))addVertex(m,{id:'REVIEW_DOT_'+(++seq),x:v.at[0]-3,y:v.at[1]-3,w:6,h:6,style:'ellipse;fillColor=#000000;strokeColor=#000000;drawioApiJunction=1;contactDot=1;'});}fs.writeFileSync(p+'.xml',serialize(doc));ev=evaluate(p,ref,id,row.name);}const r={...ev,parent,proposal};row.results.push(r);fs.writeFileSync(p+'.json',JSON.stringify(r,null,2));if(r.eligible&&better(r,best))best=r;}catch(e){row.results.push({id,parent,eligible:false,error:e.message});}}
 if(best.id===parent)break;}
 row.selected=best.id;row.selectedMetrics=[best.check.errors,best.check.crossings,best.check.warnings];const bi=row.results.findIndex(x=>x.id===best.id);if(bi>=0)row.results[bi]={...row.results[bi],...best};
 if(best.id!==before||!fs.existsSync(path.join(dir,best.id+'.png'))){const p=path.join(dir,best.id),doc=parseDrawio(fs.readFileSync(p+'.xml','utf8'));fs.writeFileSync(p+'.png',(await exportDocument(doc,getPage(doc),{scale:1.5})).buffer);}
 log.push({name:row.name,attempts,before,after:best.id,beforeRank:rank(initial),afterRank:rank(best)});fs.writeFileSync(path.join(output,'summary.json'),JSON.stringify(rows,null,2));fs.writeFileSync(path.join(output,'LOOP.json'),JSON.stringify({loop,rank:['errors','clearance',...(loop===1?['groundSpread']:loop===2?['io']:loop===3?['detour']:loop===4?['passiveJog']:['detour','io','passiveJog']),'portBends','shortJogs','bends','warnings','crossings','length'],rows:log},null,2));console.log(row.name,attempts,before===best.id?'unchanged':'IMPROVED',rank(initial),'->',rank(best));
}}finally{await closeBrowser();}
